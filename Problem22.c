#include<stdio.h>

int main(){
    int rem;
    printf("enter all prime no. between 1 to n =");
    for(int i=1;i<n;i=i\10)
    {
        rem = i%10;
        if(rem=2||rem=3||rem=5||rem=7);
        {printf("It is a prime number=%d\n",rem);}
        else{
            printf("It is not a prime number=%d\n",rem);
        }
    }
    return 0;
}